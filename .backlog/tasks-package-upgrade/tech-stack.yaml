# Tech Stack Detection
# Auto-generated by Task Manager v3.1
# Date: 2025-11-16

ecosystem: "Python"

current_environment:
  python_version: ">=3.8"
  python_version_target: ">=3.10"
  package_manager: "pip/uv"
  build_system: "hatchling"

core_dependencies:
  chromadb:
    current: ">=0.4.0"
    target: "1.3.3"
    breaking_changes: true
    migration_required: true

  sentence-transformers:
    current: ">=2.2.0"
    target: "5.1.2"
    breaking_changes: true

  PyPDF2:
    current: ">=3.0.0"
    target: "DEPRECATED"
    replacement: "pypdf>=5.2.0"
    migration_required: true

  python-docx:
    current: ">=1.0.0"
    target: "1.2.0"
    breaking_changes: false

optional_dependencies:
  PyYAML:
    current: ">=6.0"
    target: "6.0.2"
    category: "yaml"

  python-magic-bin:
    current: ">=0.4.14"
    target: "0.4.14"
    platform: "Windows"

  pinecone:
    current: ">=7.3.0"
    target: "7.3.0"
    category: "cloud-stores"

  supabase:
    current: ">=2.0.0"
    target: "3.0.1"
    breaking_changes: true
    category: "cloud-stores"

  openai:
    current: ">=1.0.0"
    target: "2.0.0"
    breaking_changes: true
    category: "cloud-embeddings"

dev_dependencies:
  pytest:
    current: ">=7.0.0"
    target: "9.0.1"
    breaking_changes: true
    python_requirement: ">=3.10"

  pytest-cov:
    current: ">=4.0.0"
    target: "6.1.0"
    breaking_changes: true

  pytest-mock:
    current: ">=3.10.0"
    target: "3.15.0"

  pytest-xdist:
    current: ">=3.0.0"
    target: "4.0.0"
    breaking_changes: true

  pytest-benchmark:
    current: ">=4.0.0"
    target: "5.2.1"
    breaking_changes: true

  ruff:
    current: ">=0.1.0"
    target: "0.9.2"
    breaking_changes: true

  mypy:
    current: ">=1.5.0"
    target: "1.14.1"

  types-PyYAML:
    current: "unspecified"
    target: "6.0.12.20250106"

  bandit:
    current: ">=1.7.0"
    target: "1.8.0"

  safety:
    current: ">=2.3.0"
    target: "3.2.12"
    breaking_changes: true

build_system:
  hatchling:
    current: "unspecified"
    target: "1.27.0"

testing_tools:
  - pytest
  - pytest-cov
  - pytest-mock
  - pytest-xdist
  - pytest-benchmark

linting_tools:
  - ruff
  - mypy
  - bandit

configuration_files:
  - pyproject.toml
  - .raggy.json (runtime config)

critical_compatibility_issues:
  - name: "Python 3.10 Requirement"
    severity: "CRITICAL"
    description: "pytest 9.x requires Python >=3.10, blocking all dev dependency upgrades"

  - name: "PyPDF2 Deprecation"
    severity: "HIGH"
    description: "PyPDF2 is deprecated and archived, must migrate to pypdf"

  - name: "ChromaDB Irreversible Migration"
    severity: "HIGH"
    description: "ChromaDB 0.4→1.3 requires database migration with no rollback option"
